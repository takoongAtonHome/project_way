# 어디쯤 · 프로토타입 실행 방법

이제 서버(Node + WebSocket)가 있으므로 **항상 서버를 통해** 실행합니다.

## 0. 서버 실행

```bash
cd project_way
npm install     # 최초 1회
npm start       # http://localhost:8000
```

- 브라우저에서 **http://localhost:8000** 접속.
- 여러 명 테스트: 모임 생성 → 초대 화면에서 **“새 탭에서 참여자로 열기”** → 두 탭 모두 “출발하기” → 서로의 실제 위치가 실시간 표시됩니다.
- 실제 경로 ETA(카카오 길찾기)를 쓰려면 REST 키를 넣어 실행:
  ```bash
  KAKAO_REST_KEY=발급받은_REST_키 npm start
  ```
  (비우면 거리 기반 추정치로 동작)

> `index.html`을 `file://`로 직접 열면 서버·GPS·카카오맵이 모두 동작하지 않습니다. 반드시 localhost로 접속하세요.
> 데스크톱이면 개발자도구(F12) → 모바일 뷰로 보면 실제 앱 느낌이 납니다.

---

## 데모 모드 vs 실제 지도 모드

- **카카오 JS 키 없음** → 데모 모드(목업 지도 + 가상 이동, 단일 사용자용 흐름 확인)
- **카카오 JS 키 있음** → 실제 지도 모드(카카오맵 + 기기 GPS + 서버 실시간 다중 사용자)

## 실제 지도 모드 (카카오맵 + 실제 GPS)

실제 지도·실제 내 위치·실제 거리/ETA로 동작시키려면 카카오 JavaScript 키가 필요합니다. (무료)

### 2-1. 카카오 JavaScript 키 발급 (약 5분)

1. [Kakao Developers](https://developers.kakao.com) 로그인 → **내 애플리케이션** → **애플리케이션 추가하기**
2. 만든 앱 → **앱 키** 에서 **JavaScript 키** 복사
3. 같은 앱 → **플랫폼** → **Web 플랫폼 등록** → 사이트 도메인에 아래를 추가
   ```
   http://localhost:8000
   ```
4. (지도 SDK 사용을 위해) **카카오맵**을 사용 설정 — 대부분 JS 키만으로 지도가 뜹니다.

### 2-2. 키 넣기

`index.html` 상단의 `CONFIG` 에서 키를 붙여넣습니다.

```js
const CONFIG = {
  KAKAO_JS_KEY: '여기에_JavaScript_키',   // ← 이 줄만 채우면 됩니다
  ...
};
```

### 2-3. 로컬 서버로 열기

카카오 SDK는 등록된 도메인에서만 동작하고, GPS(geolocation)는 보안 컨텍스트(localhost/https)에서만 동작합니다.
따라서 `file://` 로 열면 안 되고, 아래처럼 localhost 로 띄워야 합니다.

```bash
cd project_way
python3 -m http.server 8000
```

그다음 브라우저에서 접속:

```
http://localhost:8000
```

- 홈 화면 하단에 **"✅ 카카오 키 감지됨 — 실제 지도 모드"** 가 뜨면 준비 완료.
- 모임 화면에서 **"나 출발하기"** 를 누르면 브라우저가 위치 권한을 묻고, 허용하면 내 실제 위치와 목적지까지 거리·ETA가 표시됩니다.

> 📱 휴대폰 실기기에서 GPS를 테스트하려면 HTTPS 배포가 필요합니다. (다음 단계에서 배포 다룰 때 진행)

---

## 현재 단계에서 검증하는 것

- ✅ 실제 카카오맵 렌더링
- ✅ 장소명 → 좌표 변환(지오코딩)으로 목적지 지정
- ✅ 기기 실제 GPS로 내 위치 추적 + 목적지까지 실제 거리
- ⏳ ETA는 **거리 기반 추정치** (정확한 경로 ETA = 카카오 길찾기 API는 CORS로 서버가 필요 → 백엔드 단계)
- ⏳ 다른 참여자는 아직 가상 (여러 명 실시간 동기화 = 백엔드 단계)
