# 어디쯤 — 작업 기록 & 인수인계 문서

> 이 문서 하나로 다른 환경(또는 새 AI 세션)에서 작업을 이어갈 수 있도록 정리한 핸드오프 문서입니다.
> 작성: 2026-07-20 · 상태: 프로토타입(로컬 완성) → HTTPS 배포 직전

---

## 0. 한눈에 보기 (지금 어디까지 왔나)

- 아이디어 → 기획 → 화면설계 → 프로토타입 → 실제 카카오맵+GPS → 실시간 백엔드 → 배포준비 **완료**
- 로컬(`npm start`)에서 전 기능 동작 + 검증 완료
- **다음 할 일: GitHub push → Render로 HTTPS 배포 → 실기기(휴대폰)로 실제 다중 사용자 테스트**
- 로컬 git 커밋까지 완료(브랜치 `main`), remote push만 남음

---

## 1. 제품 개요

**한 줄 정의:** 모임 약속(장소·날짜·시간)을 만들고 초대하면, 당일 참석자들의 실시간 위치와 도착 예정 시간(ETA)을 지도에서 함께 보는 서비스.

**가칭:** 어디쯤

**핵심 문제(계기):**
> 모임에 먼저 도착한 사람들이 늦는 사람이 언제 올지 몰라서, 계속 기다릴지 근처로 이동할지 판단하지 못한다.

**핵심 통찰:** 본질은 "위치를 보는 것"이 아니라 **먼저 도착한 사람의 의사결정(대기 vs 이동)을 돕는 것**. 따라서 가장 중요한 정보는 정확한 위치보다 **"장소까지 몇 분 남았는가(ETA)"**.

**차별점:** 기존 위치공유(카카오톡/구글맵/Life360 등)는 전부 상시(always-on)라 부담이 큼. 이 서비스는 **모임 단위의, 목적이 분명한, 모임이 끝나면 자동으로 꺼지는 위치 공유**.

---

## 2. 확정된 결정 (이유 포함)

| 항목 | 결정 | 이유 |
|------|------|------|
| 타깃 | 친구 모임 (20~30대) | 계기가 된 상황. 앱 설치 거부감 큼 |
| 핵심 가치 | 약속 관리 통합 (단, MVP는 최소 핵심만) | 약속 관리가 위치공유를 자연스럽게 켜줌 |
| 프라이버시 | 지도 위치 공유, "출발" 버튼 눌러야 시작 | 상시공유 부담 최소화 |
| 참여자 식별 | 닉네임만 (회원가입 없음) | 설치·가입 마찰 최소화 |
| 지도/ETA | 카카오맵 + (선택)카카오모빌리티 길찾기 | 국내 대상 최적 |
| 종료 방식 | 개인은 도착 시 자동 종료 → "도착 인원" 명단 / 전체는 시간 경과 후 자동 종료 | 지도가 깔끔해지고 상황 파악 쉬움 |
| MVP 범위 | 모임 생성 + 초대 링크(닉네임 참여) + 당일 지도 위치/ETA + 도착 처리 | scope creep 경계 |
| 플랫폼 | 모바일 웹(PWA) | 설치 없이 링크로 여는 게 친구 모임에 최적 |

**의도적으로 뺀 것(향후):** 채팅, 참석투표(RSVP), 장소추천, 정산, 리마인더 알림, 백그라운드(앱 닫아도) 위치추적, 푸시 알림.

---

## 3. 진행 이력 (무엇을 왜 했는가)

1. **아이디어 구체화** — 러프한 아이디어를 문제/타깃/차별점으로 정리 → [기획서.md](기획서.md)
2. **화면 설계** — 모임생성 → 초대 → 참여 → 지도(핵심) 흐름의 와이어프레임
3. **흐름 프로토타입** — 단일 HTML, 목업 지도 + 가상 참여자 이동으로 UX 검증
4. **실물 슬라이스** — 목업을 실제 카카오맵 + 기기 GPS + 실제 거리로 교체
   - 리스크였던 "웹에서 지도+GPS+거리 성립 여부"를 가장 싸게 검증 → ✅ 통과
   - (트러블: 카카오 앱에서 "지도/로컬(OPEN_MAP_AND_LOCAL)" 서비스가 꺼져 있으면 sdk.js가 403 → 활성화로 해결)
5. **실시간 백엔드** — Node + Express + ws로 모임별 실시간 위치 릴레이 구축
   - "두 브라우저/폰이 같은 모임에서 서로 실제 위치를 본다" → 2 WS 클라이언트로 검증 ✅
6. **배포 준비** — 데이터 영속화(data.json) + Dockerfile/render.yaml + DEPLOY.md
   - 재시작해도 모임 유지 검증 ✅

---

## 4. 파일 구조

```
project_way/
├─ index.html        # 프론트엔드 전체 (단일 파일). 화면 라우팅 + 카카오맵 + GPS + WS 클라이언트
├─ server.js         # 백엔드. Express 정적서빙 + REST(모임 생성/조회) + WebSocket(위치 릴레이)
├─ package.json      # 의존성(express, ws), "npm start"
├─ Dockerfile        # 배포용 컨테이너 이미지
├─ render.yaml       # Render 배포 청사진
├─ .gitignore        # node_modules, data.json 등 제외
├─ .dockerignore
├─ 기획서.md          # 제품 기획서
├─ 실행방법.md        # 로컬 실행 & 카카오 키 설정 방법
├─ DEPLOY.md         # HTTPS 배포 가이드(Render)
└─ 작업기록.md        # (이 문서) 인수인계
```

> `node_modules/`는 git에 없음 → 새 환경에서 `npm install` 필요.
> `data.json`은 런타임 생성 파일(모임 저장소).

---

## 5. 기술 구조 요약

**프론트엔드 (index.html)**
- 화면: home / create / invite / join / map 을 `.screen.active` 토글로 전환
- `CONFIG.KAKAO_JS_KEY` 있으면 **실제 지도 모드**(카카오맵+GPS+WS), 없으면 **데모 모드**(목업 지도+가상 이동, 오프라인)
- 실제 모드: `POST /api/meetings`로 생성 → 초대링크 `?m=<id>` → WebSocket `/ws`로 join/loc/stop
- 참여자/위치/도착 상태는 **서버가 보내는 `state` 메시지가 진실의 원천**. 내 GPS는 서버로 보내고 화면은 서버 상태로 그림

**백엔드 (server.js)**
- REST: `POST /api/meetings`(생성), `GET /api/meetings/:id`(조회)
- WS 메시지: 클라→서버 `{t:'join'|'loc'|'stop'}`, 서버→클라 `{t:'joined'|'state'|'error'}`
- 서버가 목적지까지 거리(haversine) 계산 → 도착 반경(80m) 진입 시 `coming→arrived` 자동 전환
- ETA: 기본은 거리기반 추정. `KAKAO_REST_KEY` 환경변수 있으면 카카오모빌리티 길찾기 실제 소요시간 사용
- 모임 정의는 `data.json`에 영속화(참여자/연결은 휘발성이라 저장 안 함)

---

## 6. 실행 방법 (새 환경에서)

```bash
cd project_way
npm install          # 최초 1회 (node_modules 생성)
npm start            # http://localhost:8000
```
- 브라우저에서 **http://localhost:8000** (반드시 localhost. file://은 안 됨)
- 여러 명 테스트: 모임 생성 → 초대 화면 "새 탭에서 참여자로 열기" → 두 탭 다 "출발하기"
- 실제 경로 ETA: `KAKAO_REST_KEY=키 npm start`

**카카오 지도가 안 뜰 때(가장 흔한 원인):**
1. 카카오 앱에서 **지도/로컬(카카오맵) 서비스 활성화 ON**
2. **플랫폼 → Web 도메인**에 접속 주소 등록 (`http://localhost:8000`, 배포 시 배포 URL)
3. `index.html`의 `CONFIG.KAKAO_JS_KEY`에 JavaScript 키 입력

---

## 7. 현재 카카오 설정 상태

- JavaScript 키: index.html의 `CONFIG.KAKAO_JS_KEY`에 입력됨 (도메인 제한된 공개 키)
- 등록된 도메인: `http://localhost:8000`
- 지도/로컬 서비스: 활성화됨
- ⚠️ **배포하면 배포 URL을 카카오 도메인에 추가 등록해야** 지도가 뜸

---

## 8. 알려진 한계 / 기술 부채

- **데이터 영속화**: 로컬은 data.json으로 저장되나, Render 무료 플랜은 디스크가 없어 재배포 시 초기화됨 → 추후 외부 DB(예: SQLite+디스크, Postgres, Supabase 등) 필요
- **정확한 경로 ETA**: 카카오 길찾기는 서버(KAKAO_REST_KEY)에서만 호출 가능(CORS). 미설정 시 거리기반 추정
- **백그라운드 위치**: 웹은 앱을 닫으면 위치 추적 불가. 필요 시 네이티브 앱 단계
- **인증/보안**: 현재 닉네임만, 초대 링크를 아는 누구나 참여 가능(프로토타입 수준)
- **PWA 미완성**: manifest/서비스워커 아직 없음("모바일 웹 PWA" 목표 대비 남은 작업)

---

## 9. 다음 단계 (우선순위)

### 즉시 (진행 중)
1. **GitHub push** — 로컬 커밋 완료(`main`). remote만 연결하면 됨:
   ```bash
   cd project_way
   git remote add origin https://github.com/<본인계정>/project_way.git
   git push -u origin main
   ```
2. **Render 배포** — [DEPLOY.md](DEPLOY.md) 참고. 배포 후 카카오 도메인 등록 → 휴대폰 2대로 실제 위치 테스트

### 그다음 후보
3. **PWA 전환** — manifest + 서비스워커 + 홈화면 설치 (MVP 플랫폼 목표 완성)
4. **정식 네이밍 & 디자인** — 가칭 "어디쯤" 확정, 아이콘/스플래시
5. **외부 DB 전환** — 무료 배포에서도 데이터 유지
6. **기능 확장(향후)** — RSVP, 리마인더, 백그라운드 추적(네이티브)

---

## 10. 메모리 기록 (AI 세션용 컨텍스트)

> 아래는 이 프로젝트에 대해 저장된 핵심 사실 요약. 새 세션에서 이 문서를 컨텍스트로 주면 바로 이어서 작업 가능.

- 프로젝트: `project_way` = "어디쯤" (모임 실시간 위치/ETA 공유 서비스)
- 확정 방향: (2절 표 참고) 친구모임 20~30대 / 약속관리 통합(MVP 최소) / 지도위치·출발버튼 / 닉네임만 / 카카오맵 / 도착시 개인 종료+명단 / 모바일웹 PWA
- 완료: 기획서, 화면설계, 프로토타입, 실제 카카오맵+GPS 검증, Node+ws 실시간 백엔드(2 클라이언트 검증), 데이터 영속화, 배포 준비(Dockerfile/render.yaml/DEPLOY.md), 로컬 git 커밋(main)
- 다음: GitHub push → Render 배포 → 실기기 테스트 / 이후 PWA·DB·네이밍
- 주의: 배포 URL을 카카오 플랫폼 도메인에 등록해야 지도 동작. Render 무료는 디스크 없어 data.json 초기화됨.
